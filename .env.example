# App URL (where this app runs)
NEXT_PUBLIC_APP_URL=http://localhost:3000

# Session secret (min 32 characters)
SESSION_SECRET=change-me-to-a-secure-random-string-at-least-32-chars

# OAuth credentials (get these from DLAI team)
DLAI_OAUTH_CLIENT_ID=your-client-id
DLAI_OAUTH_CLIENT_SECRET=your-client-secret

# DLAI Auth Server URL
NEXT_PUBLIC_AUTH_URL=https://auth.deeplearning.ai

# For local development with ymir running locally:
# NEXT_PUBLIC_AUTH_URL=http://localhost:3001
